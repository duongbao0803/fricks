/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "firebasestorage.googleapis.com",
      "drh.vn",
      "freesvg.org",
      "cdn.pixabay.com",
    ],
  },
};

export default nextConfig;
